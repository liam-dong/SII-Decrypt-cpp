/*
 *  SII Decrypt — Format detection, AES decrypt, 3nK decode
 */

#include "sii_format.h"

#include <cstdio>
#include <cstring>
#include <cstdlib>
#include <vector>

#include "aes256.h"
#include "inflate.h"

/* --------------------------------------------------------------------------
 *  Constants
 * -------------------------------------------------------------------------- */

const uint8_t SII_KEY[32] = {
    0x2a,0x5f,0xcb,0x17,0x91,0xd2,0x2f,0xb6,0x02,0x45,0xb3,0xd8,0x36,0x9e,0xd0,0xb2,
    0xc2,0x73,0x71,0x56,0x3f,0xbf,0x1f,0x3c,0x9e,0xdf,0x6b,0x11,0x82,0x5a,0x5d,0x0a
};

const uint8_t SII_3NK_KEY_TABLE[256] = {
    
};

/* --------------------------------------------------------------------------
 *  Helpers
 * -------------------------------------------------------------------------- */

static inline uint32_t read_u32le(const uint8_t* p)
{
    return (uint32_t)p[0]
         | ((uint32_t)p[1] << 8)
         | ((uint32_t)p[2] << 16)
         | ((uint32_t)p[3] << 24);
}

/* --------------------------------------------------------------------------
 *  Format detection
 * -------------------------------------------------------------------------- */

SIIResult DetectFormat(const void* data, size_t size)
{
    if (!data || size < 4)
        return rTooFewData;

    const uint8_t* p = (const uint8_t*)data;
    uint32_t sig = read_u32le(p);

    switch (sig) {
    case SII_SIGNATURE_ENCRYPTED:
        if (size < sizeof(SIIHeader))  return rTooFewData;
        return rFormatEncrypted;
    case SII_SIGNATURE_NORMAL:
        return rFormatPlainText;
    case SII_SIGNATURE_BINARY:
        if (size < 13)  return rTooFewData;
        return rFormatBinary;
    case SII_SIGNATURE_3NK:
        if (size < SII_3NK_MIN_SIZE)  return rTooFewData;
        return rFormat3nK;
    default:
        return rFormatUnknown;
    }
}

SIIResult DetectFileFormat(const char* filename)
{
    if (!filename) return rGenericError;

    FILE* f = fopen(filename, "rb");
    if (!f) return rGenericError;

    uint8_t buf[sizeof(SIIHeader)];
    size_t n = fread(buf, 1, sizeof(buf), f);
    fclose(f);

    return DetectFormat(buf, n);
}

/* --------------------------------------------------------------------------
 *  AES-256-CBC decrypt + DEFLATE decompress
 * -------------------------------------------------------------------------- */

SIIResult DecryptData(const uint8_t* input, size_t inputSize,
                      uint8_t** outData, size_t* outSize,
                      const SIIHeader& header)
{
    *outData = nullptr;
    *outSize = 0;

    /* Step 1 — AES-256-CBC decrypt */
    std::vector<uint8_t> decrypted(inputSize);
    size_t decryptedLen = 0;

    int aesRet = aes256_decrypt_cbc(SII_KEY, header.InitVector,
                                    input, inputSize,
                                    decrypted.data(), &decryptedLen);
    if (aesRet != 0) {
        printf("  AES decrypt FAILED (error code %d)\n", aesRet);
        printf("  Ciphertext: %zu bytes (must be multiple of 16, remainder = %zu)\n",
               inputSize, inputSize % 16);
        return rGenericError;
    }

    /* Step 2 — DEFLATE decompress */
    size_t destLen = (size_t)header.DataSize;
    uint8_t* decompressed = (uint8_t*)malloc(destLen);
    if (!decompressed) return rGenericError;

    int infRet = zlib_decompress(decrypted.data(), decryptedLen,
                                 decompressed, &destLen);
    if (infRet != 0) {
        printf("  Decompress FAILED (error code %d)\n", infRet);
        printf("  Compressed: %zu bytes, expected decompressed: %zu bytes\n",
               decryptedLen, destLen);
        printf("  First 32 bytes of compressed data: ");
        for (size_t i = 0; i < 32 && i < decryptedLen; i++)
            printf("%02X ", decrypted[i]);
        printf("\n");
        free(decompressed);
        return rGenericError;
    }

    *outData = decompressed;
    *outSize = destLen;
    return rSuccess;
}

/* --------------------------------------------------------------------------
 *  3nK decode
 * -------------------------------------------------------------------------- */

void Decode3nK(const uint8_t* input, size_t size,
               uint8_t* output, uint8_t seed)
{
    for (size_t i = 0; i < size; ++i)
        output[i] = input[i] ^ SII_3NK_KEY_TABLE[(uint8_t)(seed + i)];
}
